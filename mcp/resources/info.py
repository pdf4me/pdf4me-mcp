from fastmcp.resources.function_resource import resource


@resource("info://tools")
def available_tools() -> str:
    """Describes all available tools in this MCP server."""

    return (
        "This MCP server has the following tools:\n"
        "- compress_pdf: Compress a PDF using the PDF4me API. "
        "Accepts a source PDF file path and returns compressed PDF output. "
        "Supports optimization profiles: Web, Print, and Screen.\n"
        "- flatten_pdf: Flatten a PDF using the PDF4me FlattenPdf API. "
        "Accepts a local PDF path; optional async 202 polling, output directory, and file name; saves to disk.\n"
        "- repair_pdf: Repair a PDF using the PDF4me RepairPdf API. "
        "Accepts a local PDF path; optional async 202 polling, output directory, and file name; saves the repaired PDF to disk.\n"
        "- rotate_pdf: Rotate all pages of a PDF using the PDF4me Rotate API. "
        "Accepts rotationType (NoRotation, Clockwise, CounterClockwise, UpsideDown); optional async 202 polling ; "
        "optional output directory and file name; saves the rotated PDF to disk.\n"
        "- rotate_pdf_page: Rotate selected pages of a PDF using the PDF4me RotatePage API. "
        "Requires a page spec (e.g. 1 or 1,3,5 or 2-4) and rotationType; optional async 202 polling ; "
        "optional output directory and file name; saves the PDF to disk.\n"
        "- sign_pdf: Add a signature image to a PDF using the PDF4me SignPdf API. "
        "Accepts PDF path plus signature image path; layout and margin options; optional async 202 polling ; "
        "optional output directory and file name; saves the signed PDF to disk.\n"
        "- protect_pdf: Password-protect a local PDF using the PDF4me Protect API. "
        "Requires password and pdf_permission (e.g. All); optional async 202 polling, output directory, and file name; saves the protected PDF to disk.\n"
        "- unlock_pdf: Remove password protection from a local PDF using the PDF4me Unlock API. "
        "Requires the current open password; optional async 202 polling, output directory, and file name; saves the unlocked PDF to disk.\n"
        "- linearize_pdf: Linearize a PDF using the PDF4me LinearizePdf API. "
        "Accepts a local PDF path and saves a linearized PDF. "
        "Supports optimization presets (web, Max, Print, Default, WebMax, PrintMax, PrintGray, Compress, CompressMax), "
        "optional async 202 polling, and optional output directory and file name.\n"
        "- convert_pdf_to_pdfa: Convert a PDF to PDF/A using the PDF4me PdfA API. "
        "Accepts a local PDF path, compliance (PdfA1a–PdfA3u), allow upgrade/downgrade, optional async 202 polling, "
        "and optional output path; saves the PDF/A to disk.\n"
        "- convert_html_to_pdf: Convert a local HTML file (.html/.htm) to PDF using the PDF4me ConvertHtmlToPdf API. "
        "Layout, format, scale, margins, print options, optional async 202 polling , "
        "optional output directory and file name; saves the PDF to disk.\n"
        "- convert_url_to_pdf: Convert a web URL to PDF using the PDF4me ConvertUrlToPdf API. "
        "Required input is web_url only (no local source file); auth, layout, margins, scale, optional async 202 polling; "
        "optional output directory and file name (supports output_dir/outputDir aliases); saves the PDF to disk.\n"
        "- convert_json_to_excel: Convert a local JSON file to XLSX using the PDF4me ConvertJsonToExcel API. "
        "Worksheet name, title and number/date options, optional async 202 polling , "
        "optional output directory and file name; saves the workbook to disk.\n"
        "- convert_pdf_to_excel: Convert a local PDF file to XLSX using the PDF4me ConvertPdfToExcel API. "
        "Quality (Draft/High), merge sheets, language, OCR when needed, optional async 202 polling , "
        "optional output directory and file name; saves the workbook to disk.\n"
        "- convert_pdf_to_powerpoint: Convert a local PDF file to PPTX using the PDF4me ConvertPdfToPowerPoint API. "
        "Quality (Draft/High), language, OCR when needed, merge sheets, optional async 202 polling , "
        "optional output directory and file name; saves the presentation to disk.\n"
        "- convert_md_to_pdf: Convert a local Markdown file (.md) to PDF using the PDF4me ConvertMdToPdf API. "
        "Supports optional async 202 polling , "
        "and optional output directory and file name; saves the PDF to disk.\n"
        "- convert_pdf_to_word: Convert a local PDF file to DOCX using the PDF4me ConvertPdfToWord API. "
        "Quality (Draft/High), merge sheets, language, OCR when needed, optional async 202 polling , "
        "optional output directory and file name; saves the Word document to disk.\n"
        "- convert_word_to_pdf_form: Convert a local DOCX file to a fillable PDF form using the PDF4me ConvertWordToPdfForm API. "  
        "Supports optional async 202 polling , "
        "and optional output directory and file name; saves the PDF to disk.\n"
        "- fill_pdf_form: Fill fields in a local PDF form using the PDF4me FillPdfForm API. "
        "Accepts a local PDF path plus form_data key/value JSON, supports optional async 202 polling, "
        "and optional output directory and file name; saves the filled PDF to disk.\n"
        "- get_pdf_metadata: Extract metadata from a local PDF using the PDF4me GetPdfMetadata API. "
        "Supports both sync and async processing (optional 202 polling with Location resolution), "
        "and returns structured metadata JSON (title, author, page info, dates, and security fields).\n"
        "- get_image_metadata: Extract metadata from a local JPG/PNG image using the PDF4me GetImageMetadata API. "
        "Supports both sync and async processing (optional 202 polling with Location resolution), "
        "and returns structured metadata JSON (image properties and available EXIF fields).\n"
        "- convert_image_format: Convert a local image between BMP/GIF/JPG/PNG/TIFF using the PDF4me "
        "ConvertImageFormat API. Supports sync/async processing (optional 202 polling with Location "
        "resolution), optional output directory and file name; saves the converted image to disk.\n"
        "- resize_image: Resize a local image using the PDF4me ResizeImage API (Percentage or Specific). "
        "Supports resize percentage or width/height, maintain aspect ratio, "
        "sync/async (optional 202 polling with Location resolution), optional output path; saves the image to disk.\n"
        "- rotate_image: Rotate a local image using the PDF4me RotateImage API. "
        "Supports rotation angle, background color, proportionate resize, sync/async (optional 202 polling with Location resolution), "
        "optional output directory and file name; saves the rotated image to disk.\n"
        "- convert_to_pdf: Convert a local document (e.g. DOCX, PPTX, XLSX, image, text formats) to PDF "
        "using the PDF4me ConvertToPdf API. Supports sync/async processing (optional 202 polling with "
        "Location resolution), optional output directory and file name; saves the PDF to disk.\n"
        "- convert_visio_to_pdf: Convert a local Visio file (.vsdx/.vsd/.vsdm) to PDF using the PDF4me "
        "ConvertVisio API (schemaVal=PDF). Supports PDF options, sync/async processing (optional 202 polling "
        "with Location resolution), optional output directory and file name; saves the PDF to disk.\n"
        "- create_images_from_pdf: Render PDF pages to image files using the PDF4me CreateImages API. "
        "Supports page selection (single, list, range, or all), output width in pixels, and image format "
        "(e.g. jpeg/png/tiff). Optional async 202 polling with Location resolution; saves one image per page.\n"
        "- create_swiss_qr_bill: Create a Swiss QR bill payment section on a PDF using the PDF4me CreateSwissQrBill API. "
        "Requires PDF input plus core billing fields (e.g. iban, creditor name/address type); supports optional "
        "currency/language/reference settings, optional async 202 polling, and optional output path; saves the PDF to disk.\n"
        "- create_zugferd_invoice: Create a ZUGFeRD e-invoice using the PDF4me CreateZugferdInvoice API. "
        "Supports XML/JSON/CSV invoice input, output mode (XmlWithPdf or XmlOnly), conformance level, and language; "
        "for XmlWithPdf it also accepts a source PDF. Saves PDF or XML output to disk.\n"
        "- delete_pdf_pages: Remove selected pages from a local PDF using the PDF4me DeletePages API. "
        "Accepts page expressions (e.g. 2, 1,3,5, 2-4), optional async 202 polling, and optional output path; saves the PDF to disk.\n"
        "- extract_text_from_image: Extract OCR text from a local image using the PDF4me ImageExtractText API. "
        "Supports common image formats, optional async 202 polling with Location resolution, and returns parsed extraction JSON "
        "plus a best-effort text summary.\n"
        "- generate_document_single: Generate one document from a template and data using the PDF4me GenerateDocumentSingle API. "
        "Supports template/data input modes (Docx/Html/Pdf/etc + Json/XML/Text), configurable output type (PDF/Docx/Html), "
        "optional async 202 polling, and output path selection; saves the generated file to disk.\n"
        "- generate_documents_multiple: Generate multiple documents from one template and data set using the PDF4me "
        "GenerateDocumentMultiple API. Supports Json/XML data, optional async 202 polling, and writes one or more output files "
        "to the selected directory.\n"
        "- read_barcodes: Read barcode and QR code data from a local PDF using the PDF4me ReadBarcodes API. "
        "Supports barcode type filtering and page selection; runs with async processing and returns structured barcode JSON.\n"
        "- read_barcodes_from_image: Read barcode and QR code data from a local image using the PDF4me ReadBarcodesfromImage API. "
        "Supports image type selection/inference, optional async 202 polling with Location resolution, and returns structured barcode JSON.\n"
        "- read_swiss_qr_bill: Read Swiss QR bill data from a local PDF using the PDF4me ReadSwissQRBill API. "
        "Supports optional async 202 polling with Location resolution and returns structured Swiss QR payload data as JSON.\n"
        "- remove_exif_tags_from_image: Remove EXIF metadata from a local image using the PDF4me RemoveEXIFTagsFromImage API. "
        "Supports JPG/PNG input, optional image type selection, optional async 202 polling, and output path selection; saves the cleaned image to disk.\n"
        "- replace_text_with_image: Replace text occurrences in a PDF with an image using the PDF4me ReplaceTextWithImage API. "
        "Accepts search text, page sequence, and image dimensions; supports optional async 202 polling and output path selection; saves the PDF to disk.\n"
        "- replace_text_with_image_in_word: Replace text in a Word document with an image using the PDF4me ReplaceTextWithImageInWord API. "
        "Accepts .doc/.docx input, search text, page numbers, optional first-page skip, optional async 202 polling, and output path selection; "
        "saves the modified Word file to disk.\n"
        "- rotate_image_by_exif_data: Auto-rotate a local image based on EXIF orientation using the PDF4me RotateImageByExifData API. "
        "Supports optional async 202 polling and output path selection; saves the corrected image to disk.\n"
        "- update_hyperlink_annotation: Update hyperlink annotations in a PDF using the PDF4me UpdateHyperlinkAnnotation API. "
        "Accepts search criteria and text/URL replacement values, supports optional async 202 polling and output path selection; saves the updated PDF to disk.\n"
        "- upload_file: Upload a local file to PDF4me storage using the PDF4me UploadFile API. "
        "Sends file name and base64 content, and returns structured upload response data (e.g. file reference metadata).\n"
        "- convert_ocr_pdf: Convert a PDF into a searchable/editable OCR PDF using the PDF4me ConvertOcrPdf API. "
        "Supports quality mode (Draft/High), language, OCR-when-needed behavior, optional async 202 polling, and optional output path; saves the OCR PDF to disk.\n"
    )
